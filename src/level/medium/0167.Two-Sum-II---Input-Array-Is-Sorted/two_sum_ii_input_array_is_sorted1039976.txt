// https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/solutions/1039976/rust-solution/
    let mut tree:BTreeMap<i32, i32> = BTreeMap::new();
    for (pos, e) in numbers.iter().enumerate(){
        if let Some((k, v)) = tree.get_key_value(e){
            return vec![*v + 1, (pos as i32) + 1];
        }else {
            tree.insert(target - e, pos as i32);
        }
    }
    vec![]  