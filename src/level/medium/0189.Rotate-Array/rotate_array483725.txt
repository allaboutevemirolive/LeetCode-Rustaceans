// https://leetcode.com/problems/rotate-array/solutions/483725/rust-reversing-solution/
impl Solution {
	pub fn rotate(nums: &mut Vec<i32>, k: i32) {
		let len = nums.len();
		let k = k as usize % len;
		nums.reverse();
		nums[..k].reverse();
		nums[k..].reverse();
	}
}