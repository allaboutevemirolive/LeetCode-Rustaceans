// https://leetcode.com/problems/maximum-subarray/solutions/2016585/rust/
fn max_sub_array(nums: Vec<i32>) -> i32 {
	let (_, best) = nums
		.iter()
		.skip(1)
		.fold((nums[0], nums[0]), |(acc, best), &num| {
			let acc = num.max(acc + num);
			(acc, best.max(acc))
		});
	best
}