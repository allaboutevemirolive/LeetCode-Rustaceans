// https://leetcode.com/problems/jump-game/solutions/2005534/rust/
fn can_jump(nums: Vec<i32>) -> bool {
	nums
		.into_iter()
		.enumerate()
		.scan(0, |acc, (i, num)| {
			let max = *acc;
			*acc = max.max(i + num as usize);
			Some(i <= max)
		})
		.all(|value| value)
}