// https://leetcode.com/problems/minimum-moves-to-equal-array-elements-ii/solutions/1218953/rust-3-lines/
impl Solution {
    pub fn min_moves2(mut nums: Vec<i32>) -> i32 {
        nums.sort();
        let median = nums[nums.len() / 2];
        nums.iter().map(|x| (x - median).abs()).sum()
    }
}